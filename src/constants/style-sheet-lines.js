import { StyleSheet } from 'react-native';
import {
  $green,
  $white,
  $offwhite,
  $black,
  $darkGreen,
  $deepskyblue,
} from './colors';

const styles = {
  line: {
    borderWidth: 0.2,
    borderColor: $black,
    margin: 10,
  },
  fullLine: {
    borderWidth: 0.3,
    borderColor: $black,
    opacity: 0.8,
  },
};

module.exports = styles;
