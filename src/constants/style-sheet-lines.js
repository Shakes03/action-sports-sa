import {
  $black,
} from './colors';

const styles = {
  line: {
    borderWidth: 0.2,
    borderColor: $black,
    margin: 10,
  },
  fullLine: {
    borderWidth: 0.2,
    borderColor: $black,
    opacity: 0.8,
  },
};

module.exports = styles;
